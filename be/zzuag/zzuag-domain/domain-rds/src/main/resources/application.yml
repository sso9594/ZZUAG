spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: zzuag
    password: zzuag
    url: jdbc:mysql://localhost:3306/zzuag?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&serverTimezone=Asia/Seoul
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create-drop
server:
  port: 8082
logging:
  level:
    org:
      springframework:
        security: DEBUG